import { PageComponent } from '@/common/types/page';
import { Addition } from '@/core/addition/components/Addition';

export const addition: PageComponent = () => {
  return (
    <div>
      <Addition />
    </div>
  );
};

export default addition;
